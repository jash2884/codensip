{
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "/server/server.js"
        }
    ],
    "version": 2,
    "builds": [
        {
            "src": "./server/server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/server/server.js"
        }
    ]
}
